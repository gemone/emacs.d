# Package managers
elpaca/
elpa/

# All runtime state & caches (backups, auto-saves, eln, tree-sitter,
# eshell, transient, savehist, projectile, tramp, ...)
var/

# Byte-compiled / misc
*.elc
*~

# Agent tooling runtime data
agent/
.agent-shell/
.omc/
.opencode/

# Local configuration (not tracked)
early-init.local.el
init.local.el
options.local.el
custom.el

# LSP related
.lsp-session*
lsp-mode-version-cache
.slime*

# Emacs server socket
server/
